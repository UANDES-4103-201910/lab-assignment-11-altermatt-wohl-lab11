<h1>Welcome to TicketTopia</h1>

<div class="row">
  <div class=" col-lg-2 col-md-2 col-sm-2">
    <div class="jumbotron">
      
    </div>
  </div>
  <div class="col-lg-8 col-md-8 col-sm-8">
    <div class="jumbotron">
      <h5>Tickets for the following events are currently available:</h5>
      <div class="table-responsive">
        <table class="table table-dark table-hover table-bordered">
          <thead>
            <tr>
              <th>Nombre</th>
              <th>Descripción</th>
              <th>Lugar</th>
              <th>Fecha</th>
            </tr>
          </thead>
          <tbody>
            <% @events.each do |event| %>
              <tr>
                <td><%= link_to event.name, event %></td>
                <td><%=event.description%></td>
                <td><%=event.event_venue.name%></td>
                <td><%=event.start_date.in_time_zone()%></td>
              </tr>
            <% end %>
            </tbody>
        </table>
      </div>
    </div>
    </div>    
    <div class="col-lg-2 col-md-2 col-sm-2">
      <div class="jumbotron">
        
      </div>
    </div>
    <% if can? :create, Event %>
      <button><%= link_to 'New Event', new_event_path %></button>
    <% end %>
</div>
  

<br>

